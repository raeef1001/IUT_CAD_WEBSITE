'use strict';

/**
 * intra-university-registration controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::intra-university-registration.intra-university-registration');
